# WP01 follow-up testing responsibility

The draft pull request intentionally records that tests have not been executed in the connector-only review environment. The next environment with command execution must run the repository validation commands before the pull request is made ready or merged.
