# WP01 follow-up scope

In scope:

- remove committed local preview output;
- make Service Worker registration state observable by `/dev`;
- ensure registration errors are rendered as text;
- replace ineffective tests with deterministic registration-state tests.

Out of scope:

- Firebase;
- authentication;
- Firestore;
- persistent demo data;
- Cache Storage;
- offline support;
- deployment.
